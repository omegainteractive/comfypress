.page-header
  %h2= t('.title')

- content_for :right_column do
  = render :partial => 'cms_admin/sites/mirrors', :object => @menu

= comfy_form_for @menu, :as => :menu, :url => {:action => :update} do |form|
  = render form
